{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && cd artifacts/phantom && PORT=3000 BASE_PATH=/ pnpm run build",
  "outputDirectory": "artifacts/phantom/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
