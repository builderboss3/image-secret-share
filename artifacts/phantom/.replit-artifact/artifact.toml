kind = "web"
previewPath = "/"
title = "Phantom — Secret Image Messenger"
version = "1.0.0"
id = "artifacts/phantom"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23655

[services.development]
run = "pnpm --filter @workspace/phantom run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/phantom", "run", "build" ]
publicDir = "artifacts/phantom/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23655"
BASE_PATH = "/"
