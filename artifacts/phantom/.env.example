# Optional — only needed when the frontend and API are on DIFFERENT domains
# e.g. Vercel (frontend) + Railway (API)
# If both are on Railway (same domain via /api prefix), leave this empty
VITE_API_URL=https://your-api-server.railway.app
